# سجل التحديثات (Changelog)

يوثّق هذا الملف التحديثات الجذرية على مشروع **مسطرة 2030**. الأحدث أولاً.

---

## [Security] عزل بيانات المحلات (RLS) + دفتر عملاء لكل خياط

**التاريخ:** 29 يوليو 2026

- Migration `20260729200000_multi_tenant_rls_invoices_storage.sql`: سياسات RLS على
  `invoices` (SELECT/INSERT/UPDATE/DELETE حيث `user_id = auth.uid()`)، وسياسات
  Storage لمجلد `{user_id}/` في `invoices-images`، وحذف own-row لـ `tailor_profiles`
  و`tailor_customers`.
- التطبيق يتحقق أن `user_id` في الرفع/الحفظ يطابق جلسة Supabase الحالية.
- **⚠️ Supabase:** نفّذ بالترتيب في SQL Editor:
  `20260729190000_tailor_shop_name_and_customers.sql` ثم
  `20260729200000_multi_tenant_rls_invoices_storage.sql`.

---

**التاريخ:** 29 يوليو 2026

- حُذفت مكتبة Tesseract وجميع مسارات استخراج النص من الصور.
- تدفق الفاتورة: رقم العميل + ماسح PDF/صورة فقط (بدون حقول مستخرجة تلقائياً).

---

## [Improvement] تفعيل كامل لزر «Auto» في شاشة كشف الحواف: مؤشر تحميل + إشعار فشل واضح

**التاريخ:** 29 يوليو 2026

### الطلب

تفعيل خوارزمية الاكتشاف التلقائي (Auto-Detect) خلف زر «Auto» في
`components/scanner/CornerAdjuster.tsx`: تحليل الصورة الملتقطة، تحديث
إحداثيات المقابض الزرقاء تلقائياً عند النجاح، مؤشر تحميل بسيط أثناء
التشغيل، وإشعار واضح للمستخدم مع الإبقاء على الوضع الحالي عند الفشل.

### التنفيذ

1. **نقل خوارزمية الاكتشاف إلى `CornerAdjuster.tsx`:** أُضيفت دالة مُصدَّرة
   جديدة `detectDocumentEdgesAuto(source, width, height, workCanvas)` تُغلِّف
   خوارزمية الاكتشاف الحالية (`detectDocumentQuad` — تدرّج رمادي، تحسين
   تباين محلي، عتبة Otsu تكيّفية، تنظيف مورفولوجي Close→Open، اختيار أفضل
   كتلة متصلة وفق الحجم/الامتلاء/النسيج الداخلي — بلا أي مكتبة رؤية حاسوبية
   خارجية) بمعاملات الالتقاط عالية الدقة، لتصبح نقطة الدخول الموحَّدة
   للاكتشاف التلقائي التي يستدعيها كلّ من الالتقاط الأولي وزر «Auto» صراحةً.
   تُعيد `null` صراحةً (لا تخمين) عند تعذّر العثور على حواف واثقة.
2. **مؤشر تحميل حقيقي على الزر** (`components/scanner/DocumentScannerModal.tsx`):
   عند الضغط على «Auto»، يتحوّل الزر فوراً لحالة تحميل (أيقونة دوّارة +
   نص "...جارِ الاكتشاف") ويُعطَّل مؤقتاً (`disabled`, `aria-busy`) لمنع
   الضغط المزدوج، مع مهلة قصيرة صريحة (250ms) تضمن ظهور المؤشر فعلياً
   للمستخدم قبل تحديث النتيجة (الخوارزمية نفسها سريعة جداً فعلياً).
3. **إشعار واضح عند الفشل بدل تغيير صامت:** إن تعذّر العثور على حواف واثقة،
   **يبقى شبه المنحرف الحالي كما هو دون أي تغيير** (لم يعد يُستبدَل صمتاً
   بالصورة كاملة كما كان سابقاً)، وتظهر شارة تنبيه كهرمانية أعلى شاشة كشف
   الحواف («تعذّر العثور على حواف واضحة تلقائياً. جرّب إضاءة أفضل أو خلفية
   داكنة موحّدة، أو اضبط الزوايا يدوياً.») تختفي تلقائياً بعد ~3.8 ثانية أو
   فور تفاعل المستخدم (سحب يدوي أو الضغط على «Full»).
4. **تصحيح سلوك سابق:** الاكتشاف الأولي عند الالتقاط كان يُظهر الزر «Auto»
   نشِطاً بصرياً حتى لو فشل واستُبدل بالصورة كاملة صمتاً — الآن يُصبح الوضع
   «Full» صراحةً في هذه الحالة (يعكس الواقع بدقة) مع نفس شارة التنبيه.

### الفحص والنشر

- ✅ `npx tsc --noEmit` و`npx eslint` — بلا أخطاء.
- ✅ `npm run build` — نجاح كامل.
- 🚀 نُشر التحديث على `https://www.malaktout.com`.

---

## [Redesign] مطابقة تجربة HP Smart بدقة: شاشة "كشف حواف" مستقلة بعد الالتقاط + إطار إرشادي ثابت على الكاميرا الحية

**التاريخ:** 29 يوليو 2026

### الطلب

مطابقة تصميم/تجربة الماسح تماماً لصورتَي مرجع من تطبيق HP Smart: (1) شاشة
الكاميرا الحية تعرض 4 زوايا إرشادية بيضاء ثابتة عند أطراف الإطار (توجيه
بصري بسيط، لا تتبّع حي للمستند) مع شارة "Processing..." فور الالتقاط، و(2)
شاشة "Detect Edges" منفصلة بعد الالتقاط: صورة ثابتة عالية الدقة مع شبه
منحرف أزرق متصل بأربع مقابض مربعة قابلة للسحب بدقة، وزرّي "Auto"/"Full"
أسفلها، وزر "Next" أعلى الشاشة للمتابعة.

### إعادة التصميم (`components/scanner/DocumentScannerModal.tsx`, `components/scanner/CornerAdjuster.tsx` [جديد])

1. **الكاميرا الحية مبسَّطة بإطار إرشادي ثابت فقط:** أُزيلت حلقة الاكتشاف
   الحي بالكامل (كانت تُشغَّل ~7 مرات/ثانية عبر `requestAnimationFrame`
   لرسم/تنعيم إطار تتبّع متحرّك فوق الفيديو) واستُبدلت بأربع "زوايا-L" بيضاء
   ثابتة (`CornerGuide`) بمواضع الإطار الأربعة — توجيه بصري بسيط تماماً
   كالمرجع، بلا أي تتبّع أو سحب مباشر على الفيديو (يُزيل أيضاً كل تعقيد
   التنعيم الزمني/الاستقرار/الالتقاط التلقائي السابق).
2. **شاشة "كشف الحواف" (Detect Edges) جديدة كلياً — مكوّن `CornerAdjuster`:**
   بعد الالتقاط (من الكاميرا أو المعرض)، تُرسَم الصورة الخام كاملة الدقة فوراً
   في كانفاس مخفي، ويُشغَّل اكتشاف حواف أولي سريع لتخمين شبه منحرف بادئ (أو
   الصورة كاملة إن فشل الاكتشاف)، ثم تُعرَض الصورة الثابتة (`object-contain`
   بلا قصّ) مع شبه المنحرف هذا مرسوماً بخط أزرق متصل (SVG) وأربع مقابض مربعة
   زرقاء بحدود بيضاء قابلة للسحب بحرية تامة (Pointer Events موحَّدة لمس/ماوس،
   مع ترجمة دقيقة لإحداثيات الشاشة الملتوية (Letterboxed) إلى بكسلات الصورة
   الأصلية عبر `computeContainRect`/`ResizeObserver`).
3. **زرّا "Auto" و"Full" (بأسلوب HP Smart تماماً):** "Auto" يُعيد تشغيل
   الاكتشاف التلقائي على الصورة كاملة الدقة، و"Full" يعتمد الصورة بأكملها
   حافة-إلى-حافة بلا أي اقتصاص (`fullFrameQuad`). أي سحب يدوي لأحد المقابض
   يُحوِّل الوضع إلى "يدوي" فيُلغي تفعيل الزرّين بصرياً (حالة `edgesMode`).
4. **رأس شاشة مخصَّص لكشف الحواف:** بدل زر الإغلاق (X) المعتاد، يظهر زر
   "→ رجوع" (يتجاهل الالتقاط الحالي ويعود للكاميرا الحية) وزر "التالي" البارز
   (يبدأ القصّ الهندسي الفعلي وفق شبه المنحرف المضبوط، ثم التحويل لأبيض/أسود،
   ثم شاشة المعاينة النهائية للاعتماد/إعادة الالتقاط كما كانت).
5. **تبسيط الكود:** أُزيلت كل الحالة/الثوابت الخاصة حصراً بالتتبّع الحي غير
   المُستخدَم الآن (`ENABLE_AUTO_CAPTURE`, `STABILITY_FRAME_COUNT`,
   `OVERLAY_SMOOTHING_ALPHA`, `MAX_MISSED_DETECTION_STREAK`,
   `DETECTION_INTERVAL_MS`, `FALLBACK_MARGIN_X/Y_RATIO`، ودوال رسم/سحب
   الإطار على الكانفاس الحي)، وأُزيلت أيضاً `softEnhanceCanvas` (مسار
   "غير واثق" لم يعد له داعٍ بعد وجود شاشة تعديل صريحة تجعل كل التقاط نهائي
   واثقاً بالضرورة — إمّا Auto ناجح أو Full صريح أو تعديل يدوي).

### الفحص والنشر

- ✅ `npx tsc --noEmit` — بلا أخطاء.
- ✅ `npx eslint` على كل الملفات المعدَّلة والجديدة — بلا أخطاء (أُصلح خطآن
  ظهرا أثناء التطوير: تحديث Ref أثناء الـ render، ودور ARIA "slider" ناقص
  الخصائص المطلوبة — استُبدل بـ `role="button"`).
- ✅ `npm run build` — نجاح كامل.
- 🚀 نُشر التحديث على `https://www.malaktout.com`.

---

## [Feature] ماسح مستندات بأسلوب HP Smart — 4 زوايا تفاعلية قابلة للسحب على الكاميرا الحية + حفظ PDF تلقائي

**التاريخ:** 29 يوليو 2026

### الطلب

اعتماد تجربة مسح مطابقة تماماً لتطبيق HP Smart: عرض 4 زوايا واضحة على شاشة
الكاميرا نفسها (لا بعد الالتقاط)، سحبها بحرية بالإصبع/الماوس لضبطها على
حواف المستند بدقة، ثم التقاط/قصّ/تصحيح منظور واستخراج PDF عالي الجودة مرتبط
مباشرة بنظام الرفع وقاعدة البيانات.

### التنفيذ (`components/scanner/DocumentScannerModal.tsx`, `lib/upload-scanned-invoice.ts`, `supabase/migrations/`)

1. **4 زوايا تفاعلية على البث الحي نفسه (لا شاشة تعديل منفصلة بعد الالتقاط):**
   طبقة `<canvas>` شفافة فوق الفيديو مباشرة ترسم شبه منحرف بأربع مقابض
   دائرية مرقّمة (1-4) بيضاء بحدود ملوّنة، زوايا "قوسية" (Corner Brackets)
   مثل HP Smart، وتعتيماً للمنطقة خارج المستند (`fill('evenodd')`) لإبراز
   الورقة بصرياً. اللون يتغيّر حسب الحالة: سماوي (يبحث) ← أخضر (مستقر) ←
   كهرماني (تعديل يدوي نشط) ← رمادي (إطار احتياطي مركزي).
2. **سحب حرّ للزوايا الأربع (لمس/ماوس عبر Pointer Events الموحَّدة):**
   `handleOverlayPointerDown` يحسب أقرب زاوية ضمن نصف قطر لمس مريح
   (`Math.max(52px, 7.8% من العرض)`) عبر `mapClientToVideoCoords` (يترجم
   إحداثيات الشاشة إلى بكسلات إطار الفيديو الفعلي مع مراعاة `object-cover`
   والـ letterboxing)، ثم مستمعو `pointermove`/`pointerup` على `window`
   (لا العنصر فقط) يحدّثون موضع الزاوية المسحوبة ويعيدون رسم الإطار مباشرة
   على الكانفاس (بلا مرور بـ React state في كل بكسل حركة، للسلاسة القصوى).
   أي لمسة على زاوية تُفعِّل `userAdjustedQuadRef` فيُعتمد هذا الإطار اليدوي
   حصراً بدل أي اكتشاف تلقائي لاحق حتى إعادة الالتقاط.
3. **التقاط ومعالجة مبنية على الزوايا المضبوطة فعلياً:** زر الالتقاط (أو
   النقر في أي مكان على الشاشة) يستخدم مباشرة `manualQuadRef` إن عدّل
   المستخدم الزوايا، وإلا فآخر إطار مُكتشَف تلقائياً (`smoothedQuadRef`)، ثم
   `warpPerspective` (Homography كامل + أخذ عيّنة ثنائية الخطية) لتصحيح
   المنظور فعلياً بناءً على هذه الزوايا بالذات، تليها `enhanceDocumentCanvas`
   (تحويل أبيض/أسود عالي التباين) عند الثقة الكاملة.
4. **حفظ PDF تلقائي فور المعالجة (بلا شاشة معاينة وسيطة عند الثقة الكاملة):**
   بمجرد اكتمال القصّ/التصحيح بثقة (اكتشاف تلقائي ناجح أو تعديل يدوي صريح)،
   يُستدعى `saveProcessedDocument` تلقائياً: تحويل الكانفاس لـ PDF بدقة عالية
   (200ppi عبر `canvasToDocumentPdfBlob({ highQuality: true })`)، ثم الرفع
   والحفظ فوراً — تجربة "التقاط = حفظ" مباشرة كما في HP Smart، مع تراجع
   لشاشة "preview-uncertain" (معاينة + تحذير صريح) فقط في الحالات النادرة
   غير الواثقة تماماً (لا اكتشاف تلقائي ولا تعديل يدوي).
5. **الرفع والحفظ في قاعدة البيانات (`lib/upload-scanned-invoice.ts`):**
   `uploadScannedInvoiceFiles` يرفع كلاً من ملف PDF (المصدر الرسمي) وصورة
   JPEG (معاينة مصغّرة) إلى Supabase Storage (bucket: `invoices-images`) في
   نفس المسار المُوقَّت، ويعيد الروابط العامة لكليهما. `insertInvoiceRecord`
   يُدرج سجلّ الفاتورة في جدول `invoices` بعمود `pdf_url` الجديد (مع خطأ
   واضح يوجّه لتشغيل الـ migration إن كان العمود مفقوداً بعد). أُضيف عمود
   `pdf_url` عبر `supabase/migrations/20260729140000_add_invoices_pdf_url.sql`.
   تنزيل/طباعة الفاتورة (`app/page.tsx`) يفضّلان `pdf_url` تلقائياً عند
   توفّره (`invoiceShareDocumentUrl`).

### ⚠️ خطوة يدوية مطلوبة من المالك (مرة واحدة فقط)

يجب تنفيذ ملف الـ migration
(`supabase/migrations/20260729140000_add_invoices_pdf_url.sql`) في **لوحة
تحكم Supabase → SQL Editor** إن لم يكن عمود `pdf_url` موجوداً بعد فعلياً في
جدول `invoices` على قاعدة بيانات الإنتاج — الكود يكتشف غياب العمود ويعرض
رسالة خطأ عربية واضحة توجّه لهذه الخطوة بالضبط بدل الفشل الصامت.

### الفحص والنشر

- ✅ `npx tsc --noEmit` — بلا أخطاء.
- ✅ `npx eslint` على كل ملفات الماسح الضوئي والرفع — بلا أخطاء (الأخطاء
  الموجودة في `app/page.tsx`/`app/auth/callback/page.tsx` سابقة الوجود وغير
  متعلقة بهذا التحديث).
- ✅ `npm run build` — نجاح كامل.
- 🚀 نُشر التحديث على `https://www.malaktout.com`.

---

## [Fix] إزالة جذرية لمكتبة OpenCV.js من مسار الماسح الضوئي — اعتماد حصري على الخوارزمية اليدوية المستقرة

**التاريخ:** 28 يوليو 2026

### المشكلة

رغم إصلاحات المهلة الزمنية والتحميل المؤجَّل السابقة، استمرّ المستخدم في
ملاحظة تعليق/تجمّد شاشة "جاري تشغيل الكاميرا..." على بعض الأجهزة. السبب
الجذري المُحتمَل: تحميل/تهيئة ملف WASM ضخم (~10MB) عبر `opencv.js` يبقى
عبئاً كبيراً وغير مضمون التوقيت على المعالج الرئيسي لأي متصفح/جهاز مهما
أُجِّل، إضافة إلى تعقيد إضافي غير ضروري في مسار حرِج (فتح الكاميرا).
تقرَّر إزالة الاعتماد على OpenCV.js كلياً بدل الاكتفاء بتخفيف أثره.

### الإصلاح

1. **حذف كامل لملفات OpenCV.js وكل الشيفرة المرتبطة بها:**
   - حُذفت الملفات: `lib/document-scanner/opencv-loader.ts`،
     `lib/document-scanner/cv-detect.ts`، `lib/document-scanner/cv-warp.ts`،
     ومجلد `public/opencv/` بالكامل (ملف الـ WASM الذاتي الاستضافة والترخيص).
   - حُذفت دالة `enhanceDocumentCanvasCv` (والدوال المساعدة `deleteAll`/
     `oddClamp` الخاصة بها فقط) من `lib/document-scanner/enhance.ts`.
   - حُذفت كل الثوابت الخاصة بـ OpenCV.js من `constants.ts`
     (`CV_DETECTION_SAMPLE_WIDTH`, `CV_CAPTURE_DETECTION_SAMPLE_WIDTH`,
     `CV_CANNY_*`, `CV_APPROX_POLY_EPSILON_RATIO`, `CV_ADAPTIVE_*`).
   - أُزيل حقن سكربت `opencv.js` نهائياً (لا حقن ديناميكي ولا ساكن) — لا
     يُحمَّل أي ملف WASM إطلاقاً أثناء فتح الكاميرا أو معالجة المستند بعد الآن.
2. **اعتماد حصري على الخوارزمية اليدوية الخفيفة (`ImageData` + Canvas API القياسي)**
   في `components/scanner/DocumentScannerModal.tsx`: أُزيلت دالة التغليف
   `detectQuadUnified` واستُبدلت باستدعاء مباشر لـ `detectDocumentQuad` (حلقة
   الاكتشاف الحي ولحظة الالتقاط)، ولـ `warpPerspective` اليدوي لتصحيح
   المنظور، ولـ `enhanceDocumentCanvas`/`softEnhanceCanvas` للتحسين — كل ما
   كان يعمل مسبقاً (الإطار التفاعلي، الالتقاط اليدوي/التلقائي، تحويل
   الأبيض/الأسود) يستمر بكامل طاقته دون أي تغيير في السلوك أو الجودة.
3. **الإبقاء على كل إصلاحات الاستقرار السابقة كما هي:** مهلة تشغيل الكاميرا
   الصريحة (`CAMERA_START_TIMEOUT_MS = 8000`)، وزر الإغلاق (X) المستقل تماماً
   (`fixed`, `z-[100]`, `isClosingRef` مع فحوصات الخروج المبكر في كل عملية
   غير متزامنة) — هذه الإصلاحات لا علاقة لها بـ OpenCV.js وتبقى ضرورية
   لضمان استجابة فورية بغضّ النظر عن سبب أي تعليق مستقبلي.

### النتيجة

موقع أخف وأسرع تحميلاً (لا حزمة WASM ضخمة إطلاقاً)، كاميرا تفتح فوراً بلا أي
تنافس على المعالج، زر إغلاق يستجيب دوماً بشكل مطلق، ومسح ضوئي يعمل بكامل
طاقته عبر الخوارزمية اليدوية المستقرة (دقة جيدة رغم كونها أقل من OpenCV في
الحالات القصوى، لكنها موثوقة ومضمونة الأداء على كل الأجهزة).

### الفحص والنشر

- ✅ `npx tsc --noEmit` — بلا أخطاء.
- ✅ `npx eslint` على كل ملفات الماسح الضوئي — بلا أخطاء.
- ✅ `npm run build` — نجاح كامل.
- 🚀 نُشر التحديث فوراً على `https://www.malaktout.com`.

---

## [Fix] إصلاح تعليق شاشة "جاري تشغيل الكاميرا..." + ضمان استقلالية زر الإغلاق

**التاريخ:** 28 يوليو 2026

### المشكلة

بلّغ المستخدم عن تجمّد الشاشة بالكامل على "جاري تشغيل الكاميرا..." بلا أي
استجابة (حتى زر الإغلاق X)، على الأرجح بسبب تحميل/تهيئة OpenCV.js (~10MB
WASM) بالتزامن الدقيق مع طلب إذن الكاميرا وبدء تشغيلها — فتنافسا على معالج
الجهاز الرئيسي في نفس اللحظة الحرجة.

### الإصلاح (`components/scanner/DocumentScannerModal.tsx`, `opencv-loader.ts`, `constants.ts`)

1. **تأجيل تحميل OpenCV.js لما بعد نجاح تشغيل الكاميرا فعلياً** (`phase === 'live'`)
   بدل تحميله فور فتح الماسح مباشرة — يفصل المسارين زمنياً تماماً فلا
   يتنافسان على المعالج الرئيسي في لحظة بدء الكاميرا الحرجة، بينما يبقى
   المحرّك على الأرجح جاهزاً بحلول لحظة الالتقاط الفعلي.
2. **مهلة زمنية صريحة لتشغيل الكاميرا** (`CAMERA_START_TIMEOUT_MS = 8000`):
   إن لم تُحلّ نافذة إذن الكاميرا أو يبدأ البث الفعلي خلال 8 ثوانٍ، تظهر
   رسالة خطأ واضحة ("تعذّر تشغيل الكاميرا خلال المهلة المسموحة...") مع خياري
   «إعادة المحاولة» و«من المعرض» بدل تعليق الشاشة إلى ما لا نهاية. أي بث
   كاميرا يصل متأخراً بعد انتهاء المهلة يُحرَّر فوراً (إيقاف المسارات) بدل
   تركه يعمل بلا استخدام.
3. **تقليل مهلة تحميل OpenCV.js** من 15 إلى 8 ثوانٍ لتراجع أسرع لخط الأنابيب
   اليدوي عند شبكة بطيئة.
4. **زر الإغلاق (X) مستقل تماماً وقابل للنقر تحت أي ظرف:** أصبح موضعه
   `fixed` (لا `absolute`) بأعلى ترتيب طبقات ممكن (`z-[100]`)، مستقلاً كلياً
   عن أي طبقة تحميل/معالجة/خطأ داخل الماسح. عند الضغط عليه: يُضبَط علم
   إغلاق فوري (`isClosingRef`) يمنع أي عملية غير متزامنة قيد التنفيذ (بدء
   الكاميرا، مهلتها، اكتشاف/التقاط) من تحديث الحالة بعد ذلك مهما كانت
   المرحلة، تُلغى مهلة الكاميرا فوراً، يُوقَف بثّ الكاميرا (`MediaStream`)،
   ثم يُغلَق الماسح ويعود المستخدم للصفحة السابقة — كل ذلك بشكل متزامن فوري
   بلا انتظار أي Promise معلَّق.

### النتيجة

لا يمكن للشاشة أن تتجمّد على "جاري تشغيل الكاميرا..." لأكثر من 8 ثوانٍ كحدّ
أقصى، وزر الإغلاق يعمل فوراً في كل الأحوال بمعزل تام عن أي حالة تحميل داخلية.

---

## [Feature] ترقية الماسح الضوئي لمحرّك رؤية حاسوبية حقيقي (OpenCV.js) مع تراجع آمن كامل

**التاريخ:** 28 يوليو 2026

### الهدف

استبدال خط الأنابيب اليدوي (Canvas 2D خالص، تصنيف حسب السطوع) بمحرّك رؤية
حاسوبية حقيقي ومُثبت عالمياً — **OpenCV.js** (WebAssembly) — لرفع دقة اكتشاف
زوايا المستند الأربع، وتصحيح المنظور، والتحسين النهائي، مع **الإبقاء الكامل**
على خط الأنابيب اليدوي السابق كشبكة أمان احتياطية تلقائية في كل مرحلة.

### التغييرات

1. **استضافة ذاتية لملف OpenCV.js** (`public/opencv/opencv.js`, ~10MB): بدل
   استيراده كحزمة npm عبر الحزمة (Bundler) — قد يتعارض مع فروع كود Node
   الداخلية للملف (`require('fs')`/`require('crypto')`) وتُضخِّم حزمة
   JavaScript الأساسية — يُحقَن كوسم `<script>` كلاسيكي **فقط عند فتح الماسح
   الضوئي فعلياً** (`lib/document-scanner/opencv-loader.ts`)، فلا يؤثر إطلاقاً
   على سرعة تحميل بقية التطبيق. استضافته ذاتياً (لا CDN خارجي) يضمن موثوقية
   تامة في الإنتاج.
2. **اكتشاف حواف حقيقي عبر Contour Detection** (`lib/document-scanner/cv-detect.ts`):
   تدرج رمادي → تمويه Gaussian → كشف حواف **Canny** (حدود/خطوط حقيقية، لا
   فرق سطوع فقط) → تمديد خفيف لسدّ الثغرات → استخراج كل المخططات
   (`findContours`) → تقريب مضلّعي (`approxPolyDP`) → اختيار أكبر مضلّع رباعي
   محدَّب بتغطية معقولة. هذا يتعامل بدقة أعلى بكثير مع مستندات مائلة أو
   خلفيات معقّدة/قريبة الإضاءة من الورقة (المشكلة السابقة تحديداً)، لأن حافة
   الورقة الفعلية تُنتج خطاً حاداً يكتشفه Canny حتى مع تشابه السطوع.
3. **تصحيح منظور حقيقي (Homography)** (`lib/document-scanner/cv-warp.ts`):
   `cv.getPerspectiveTransform` + `cv.warpPerspective` بمعالجة WASM محسَّنة
   (أسرع وأدق من حلقة JS يدوية بكسلاً-بكسل عند دقات إخراج كبيرة).
4. **تحسين ذكي (Smart Binarization)** (`enhanceDocumentCanvasCv` في
   `enhance.ts`): عتبة تكيّفية محلية حقيقية عبر `cv.adaptiveThreshold`
   (`ADAPTIVE_THRESH_GAUSSIAN_C`) — الأسلوب المعياري في تطبيقات المسح
   الضوئي الاحترافية — يليها فتح مورفولوجي خفيف لتنظيف الضجيج المتبقي.
5. **تراجع آمن (Graceful Fallback) في كل مرحلة بلا استثناء:** كل دالة
   OpenCV.js (`detectDocumentQuadCv`, `warpPerspectiveCv`, `enhanceDocumentCanvasCv`)
   تتحقق أولاً من جاهزية المحرّك وتُرجع `null`/`false` بأمان تام عند عدم
   التوفّر أو أي خطأ (شبكة بطيئة، متصفح قديم...) — فيتراجع الماسح تلقائياً
   وفورياً لخط الأنابيب اليدوي الأصلي (نفس المنطق المستقر السابق بالكامل،
   بلا حذف) دون أي تعطّل ملحوظ للمستخدم.
6. **PDF مضغوط بكفاءة أعلى** (`to-pdf.ts`): تفعيل ضغط jsPDF الداخلي
   (`compress: true`)، مع تضمين الصورة بصيغة **PNG** (ضغط Deflate بلا فقد)
   بدل JPEG حصراً للمستندات المُحوَّلة فعلياً لأبيض/أسود عالي التباين —
   مناطقها المسطّحة الواسعة تضغط أصغر بكثير من JPEG بلا أي فقد أو تشويش حواف
   حول الحروف الحادة (Ringing Artifacts)، فتقلّ مساحة تخزين Supabase Storage
   المستهلكة مع وضوح تام للنصوص. تبقى المعاينات الاحترازية (غير الواثقة)
   بصيغة JPEG لأنها صور فوتوغرافية ملوّنة عادية تضغط أفضل بهذه الصيغة.

### النتيجة

اكتشاف حواف أدق بكثير (خصوصاً مع خلفيات معقّدة/مستندات مائلة)، تصحيح منظور
وتحسين أسرع وأعلى جودة عبر WASM، ملفات PDF أصغر حجماً بلا خسارة وضوح، مع
موثوقية كاملة عبر تراجع تلقائي فوري لخط الأنابيب اليدوي المستقر عند أي عائق.

---

## [Fix] رفض اكتشاف "ورقة+خلفية مدموجتين" + تراجع آمن بدل استخدام الكاميرا كاملة

**التاريخ:** 28 يوليو 2026

### المشكلة

عند تصوير مستند فوق خلفية فاتحة قريبة الإضاءة من الورقة (مثل أرضية بلاط/سيراميك
فاتحة)، كانت خوارزمية الاكتشاف تدمج الورقة والخلفية في كتلة "ساطعة" واحدة
ضخمة (لأن الفرق بينهما بالسطوع ضئيل)، فيُعتمَد شبه الإطار بأكمله (الكاميرا
بكامل طولها) كأنه "المستند"، ثم يُطبَّق عليه تحويل المسح الثنائي (أبيض/أسود)
القاسي على كامل تلك الخلفية الحقيقية (أرضية، أطراف قدم...) فيُنتج ضجيجاً
كثيفاً يشوّه الناتج بالكامل — تماماً كما ظهر في اختبار المستخدم.

### الإصلاح

1. **رفض الكتل المدموجة عبر فحص النسيج الداخلي (`detect-document.ts`):**
   إضافة حساب خارطة تدرّج (Sobel Gradient Magnitude) وقياس متوسط "نسيج"
   منتصف الكتلة المرشَّحة (بعيداً عن حوافها). سطح الورقة الداخلي مسطّح
   (نسيج منخفض)، بينما خلفية كأرضية بلاط تحمل فواصل/نقوش كثيفة (نسيج مرتفع)
   — أي كتلة يتجاوز نسيجها `MAX_INTERIOR_TEXTURE` تُرفَض فوراً حتى لو
   اجتازت فحوصات التغطية والامتلاء السابقة.
2. **تشديد أعلى نسبة تغطية مقبولة:** خفض `MAX_COVERAGE_RATIO` من `0.92` إلى
   `0.7` — أي كتلة تقترب من تغطية الإطار بأكمله تُعامَل بريبة أكبر.
3. **تراجع آمن بدل الإطار الكامل (`DocumentScannerModal.tsx`):** عند تعذّر
   اكتشاف حواف بثقة كافية عند الالتقاط، لم نعد نستخدم حرفياً حواف-إلى-حواف
   الكاميرا (وما تحمله من أرضية/أثاث/أطراف الجسم)؛ بل نتراجع إلى إطار مركزي
   احترازي أضيق (`centeredFallbackQuad`) يحذف هامشاً من كل جهة.
4. **تعطيل تحويل المسح الثنائي عند عدم الثقة (`enhance.ts`):** أُضيفت دالة
   `softEnhanceCanvas` (تباين/سطوع لطيفان فقط، بلا تحويل أبيض/أسود قاسٍ).
   يُستخدم `enhanceDocumentCanvas` الحاد فقط عندما ينجح اكتشاف دقيق وطازج
   فعلياً (`confident = true`)؛ غير ذلك يُستخدم `softEnhanceCanvas` لتفادي
   الضجيج الكثيف فوق أي خلفية واقعية غير مسطّحة.
5. **تنبيه صريح للمستخدم:** أُضيفت حالة عرض جديدة (`preview-uncertain`) تُظهر
   رسالة تحذير واضحة في شاشة المعاينة عند عدم الثقة بدقة الحواف، تقترح إعادة
   الالتقاط مع خلفية داكنة موحّدة وإضاءة أفضل، مع إبقاء زرَّي "إعادة الالتقاط"
   و"اعتماد المستند وحفظه" متاحين كما هما.

### النتيجة

لن يظهر بعد الآن ذلك الضجيج الأسود الكثيف الذي يغطي الصورة بالكامل عند فشل
الاكتشاف؛ إما يُكتشف المستند بدقة وتُطبَّق عليه معالجة المسح الضوئي الكاملة،
أو يُعرض قصّ مركزي محافظ مع تحسين لطيف وتنبيه صريح للمستخدم لإعادة المحاولة.

---

## [Fix] معالجة "الضجيج الأسود" في الخلفية + استقرار أعلى للإطار التفاعلي

**التاريخ:** 28 يوليو 2026

### الهدف

معالجة ملاحظة أن فلتر التباين القوي كان يحوّل الخلفية المحيطة بالورقة إلى
ضجيج أسود كثيف يشوّه المستند ويمنع تحديد حوافه بدقة، عبر تحسين خوارزمية
اكتشاف الحواف نفسها (لا الاكتفاء بتلطيف الإخراج النهائي فقط) بالإضافة إلى
تنظيف صريح للخلفية وتثبيت الإطار التفاعلي.

### 1) تحسين اكتشاف الحواف (`lib/document-scanner/detect-document.ts`)

- **تنعيم أولي (Pre-blurring)** خفيف جداً (تمويه صندوقي بنصف قطر 1، شبيه
  بـ Gaussian) على تدرج الرمادي قبل أي تحليل — يزيل ضجيج مستشعر الكاميرا.
- **تحسين تباين محلي**: تطبيع كل بكسل بالنسبة لإضاءة محيطه المباشر (تمويه
  صندوقي أكبر نطاقاً، بنفس فكرة تطبيع الإضاءة المستخدمة في `enhance.ts`)
  قبل حساب عتبة Otsu — يجعل حواف الورقة البيضاء أبرز وأوضح تمايزاً عن
  الخلفية الداكنة مهما كانت الإضاءة غير متساوية.
- **إغلاق ثم فتح مورفولوجي (Close → Open)** بدل الإغلاق فقط سابقاً: الإغلاق
  يسدّ الثغرات داخل الورقة، والفتح الجديد يُزيل بقع الضجيج الصغيرة المتناثرة
  في الخلفية (يُطبَّق فقط لحظة الالتقاط الفعلي — مرة واحدة، فلا يؤثر على أداء المعاينة الحية).
- **اختيار من بين عدة كتل مرشَّحة لا كتلة واحدة فقط**: أصبحت الخوارزمية
  تبحث عن كل الكتل المتصلة الكبيرة بما يكفي، ثم تُفضِّل بينها الكتلة ذات
  "نسبة امتلاء" عالية (شكلها قريب من مستطيل صلب) قبل حجمها — هذا يمنع
  اختيار بقعة ضجيج متناثرة كبيرة عرضاً بدل الورقة الفعلية.

### 2) تنظيف الخلفية في الصورة النهائية (`lib/document-scanner/enhance.ts`)

- **عتبة تباين منحازة نحو الأبيض (`INK_BIAS`)**: بدل استخدام عتبة Otsu
  كما هي، تُخفَّض بمقدار ثابت — فيصبح تصنيف بكسل كـ"حبر أسود" أكثر صرامة
  (يتطلب دكانة أوضح)، بينما يصبح تصنيفه كـ"ورقة بيضاء" أكثر تسامحاً، فتقلّ
  حالات "الضجيج الأسود" الزائف بشكل واضح.
- **تنظيف نهائي (Despeckle)**: تمريرة جديدة تبحث عن أي بقع سوداء صغيرة
  معزولة (أصغر من حجم أدنى مضبوط تناسبياً مع حجم الصورة) وتحوّلها لأبيض
  نقي تلقائياً، دون المساس بالنصوص الحقيقية (كتل أكبر بكثير).
- تخفيف طفيف لحدّة منحنى التباين (Sigmoid) لتقليل تضخّم الضجيج القريب من نقطة العتبة.

### 3) استقرار الإطار التفاعلي (`components/scanner/DocumentScannerModal.tsx`)

- **تنعيم زمني (Exponential Moving Average)** لموضع زوايا الإطار المرسوم
  بين الإطارات المتتالية — يقلّل الاهتزاز البصري الناتج عن ضجيج اكتشاف
  بسيط، خصوصاً في الإضاءة الضعيفة أو الخلفية المضطربة.
- **فترة سماح** لعدة محاولات اكتشاف فاشلة متتالية (بدل إخفاء الإطار فوراً
  عند أول فشل) — يمنع الوميض المزعج للإطار.
- **ملاحظة مهمة**: هذا التنعيم يخصّ فقط رسم الإطار الحي وفحص "الثبات"
  لأغراض الالتقاط التلقائي؛ دقة القصّ النهائي تبقى كما في التحديث السابق
  معتمدة دوماً على اكتشاف طازج عالي الدقة يُحسَب لحظة الالتقاط الفعلي، لا
  على هذا التنعيم المرئي.

### الفحص والتأكد

- ✅ `npx tsc --noEmit` — بدون أخطاء أنواع.
- ✅ `npx eslint` على ملفات الماسح الضوئي — بدون أخطاء أو تحذيرات.
- ✅ `npm run build` — بناء إنتاجي ناجح بالكامل.
- ⚠️ يُنصح باختبار ميداني على أسطح متعددة (طاولة فاتحة/داكنة/قماش) بعد
  النشر لضبط ثوابت التحسّس (`INK_BIAS`, `MIN_BLOB_FILL_RATIO`, حجم البقع
  المُزالة) بدقة أكبر إن لزم — جميعها مركزّة في `lib/document-scanner/constants.ts`
  لتسهيل أي معايرة لاحقة سريعة دون تعديل منطق الخوارزمية.

---

## [Fix] تأثير مسح ضوئي حقيقي (أبيض/أسود) + إصلاح جمود زر "إعادة الالتقاط"

**التاريخ:** 28 يوليو 2026

### 1) تأثير الماسح الضوئي الحقيقي (Scan Effect / Adaptive Thresholding)

- إعادة كتابة `lib/document-scanner/enhance.ts` بالكامل: بدل رفع تباين/سطوع
  بسيط على الصورة الملوّنة، أصبحت الآن تُنتج **مستنداً أبيض/أسود عالي
  التباين حقيقياً** يحاكي مخرجات ماسحات ضوئية فعلية أو تطبيقات مثل
  CamScanner/Adobe Scan:
  1. تحويل إلى تدرج رمادي (يُزيل تلقائياً أي بصمة لونية/إضاءة صفراء).
  2. **تطبيع الإضاءة المحلية**: تمويه صندوقي كبير النطاق (Box Blur بنافذة
     منزلقة أفقياً/رأسياً — تكلفة O(n) ثابتة بغضّ النظر عن نصف القطر) يُقدِّر
     إضاءة كل منطقة من الصفحة، ثم قسمة كل بكسل على إضاءته المحلية — يُزيل
     الظلال والتفاوت في الإضاءة عبر الصفحة تماماً بدل بقائها كبقعة داكنة.
  3. **عتبة Otsu تلقائية** على الناتج المُطبَّع تتكيّف مع كل مستند بعينه
     (بدل رقم ثابت لا يناسب كل الإضاءات).
  4. **منحنى تباين حاد (Sigmoid)** حول تلك العتبة يدفع الخلفية إلى أبيض نقي
     (255) والنص إلى أسود داكن تماماً (0)، مع الإبقاء على درجة تنعيم بسيطة
     عند الحواف بدل تسطيح ثنائي القيمة صارم يبدو مسنَّناً.
- النتيجة مطبَّقة تلقائياً على كل مستند يُلتقط (كاميرا حية أو من المعرض)
  قبل تحويله إلى JPEG/PDF ورفعه.

### 2) إصلاح جمود زر "إعادة الالتقاط" (Retake)

- **السبب الجذري:** كان عنصر `<video>` وكانفاس الإطار التفاعلي يُزالان
  بالكامل من شجرة الـ DOM (Unmount) عند الانتقال لمرحلة المعاينة (`preview`).
  عند الضغط على "إعادة الالتقاط" والعودة لمرحلة `live`، كان React يُركِّب
  عنصر `<video>` **جديداً تماماً** بلا أي `srcObject` مرتبط به (البث
  `MediaStream` كان لا يزال يعمل فعلياً في الخلفية، لكنه غير موصول بهذا
  العنصر الجديد) — فتظهر شاشة سوداء/مجمّدة رغم أن الكاميرا تعمل.
- **الإصلاح:** أصبح عنصرا `<video>` والكانفاس التفاعلي **مثبَّتين دائماً**
  طوال حياة مكوّن الماسح (`DocumentScannerModal`)، ويُخفيان بصرياً فقط
  (`invisible`) في مراحل المعاينة/الخطأ بدل إزالتهما من الشجرة. هذا يضمن
  بقاء `srcObject` متصلاً بشكل مستمر، فتعود المعاينة الحية فوراً وبسلاسة
  تامة دون أي تجميد عند "إعادة الالتقاط".
- تنظيف إضافي: مسح أي رسم متبقٍّ على الكانفاس التفاعلي وإعادة ضبط حالة
  الاكتشاف قبل العودة للبث الحي، لتفادي وميض إطار قديم للحظة.

### الفحص والتأكد

- ✅ `npx tsc --noEmit` — بدون أخطاء أنواع.
- ✅ `npx eslint` على ملفات الماسح الضوئي — بدون أخطاء أو تحذيرات.
- ✅ `npm run build` — بناء إنتاجي ناجح بالكامل.

---

## [Improvement] دقة أعلى للالتقاط + شاشة اعتماد صريحة في الماسح الضوئي

**التاريخ:** 28 يوليو 2026

### الهدف

معالجة ملاحظة أن الاكتشاف الحي للحواف (منخفض الدقة، لأغراض الأداء الحي)
قد يكون غير دقيق أحياناً كأساس وحيد للقص النهائي، وجعل خطوة المراجعة قبل
الحفظ صريحة لا لبس فيها.

### 1) اكتشاف حواف دقيق ومنفصل في لحظة الالتقاط بالذات

- `lib/document-scanner/detect-document.ts`: أصبحت `detectDocumentQuad` تقبل
  إعدادات اختيارية (`sampleWidth`, `denoise`)، مع إضافة **إغلاق مورفولوجي**
  (Dilate ثم Erode) اختياري لسدّ الثغرات الصغيرة (ظلال/انعكاسات) داخل قناع
  الورقة الثنائي قبل البحث عن أكبر منطقة متصلة — يرفع دقة تحديد الحواف
  بوضوح.
- ثابت جديد `CAPTURE_DETECTION_SAMPLE_WIDTH` (640px) في `constants.ts`
  مخصص **فقط** للحظة الالتقاط الفعلية (أعلى دقة بكثير من الـ 220px
  المستخدمة في الحلقة الحية المستمرة)، مع تفعيل `denoise` لأنه يُشغَّل مرة
  واحدة فقط فلا يؤثر على سلاسة المعاينة الحية.
- `DocumentScannerModal.tsx`: عند الضغط على زر الالتقاط اليدوي، أو النقر في
  أي مكان على شاشة الكاميرا (ميزة جديدة — تماماً كالضغط على زر الالتقاط)،
  أو عند الالتقاط التلقائي لثبات الإطار، تُجري `finishCapture` الآن **دائماً**
  اكتشافاً دقيقاً وطازجاً على كامل دقة الصورة الملتقطة في تلك اللحظة بالضبط
  (بدل الاعتماد على آخر اكتشاف حي منخفض الدقة/المتأخر)، ولا يُستخدم اكتشاف
  الحلقة الحية إلا كشبكة أمان احتياطية إن تعذّر اكتشاف دقيق تماماً.

### 2) شاشة اعتماد صريحة (Confirmation Step)

- شاشة المعاينة بعد الالتقاط (موجودة أصلاً) وُضِّحت رسالتها صراحةً:
  "راجع وضوح المستند والحواف قبل الاعتماد النهائي — لن يُحفظ أو يُرسل تلقائياً".
- إعادة تسمية الزرّين ليكونا: **"✅ اعتماد المستند وحفظه"** (يبدأ الرفع الفعلي
  فقط عند الضغط عليه) و **"🔁 إعادة الالتقاط"** (يعيد فتح الكاميرا الحية دون
  حفظ أي شيء)، بالإضافة إلى زر إغلاق (✕) عام متاح دوماً في الأعلى للإلغاء
  الكامل من أي مرحلة.

### الفحص والتأكد

- ✅ `npx tsc --noEmit` — بدون أخطاء أنواع.
- ✅ `npx eslint` على ملفات الماسح الضوئي — بدون أخطاء أو تحذيرات.
- ✅ `npm run build` — بناء إنتاجي ناجح بالكامل.

---

## [Feature] الماسح الضوئي الذكي للمستندات (Smart Document Scanner)

**التاريخ:** 28 يوليو 2026

### الهدف

استبدال التقاط الصور العادية (`<input type="file" capture="environment">`)
بتجربة "ماسح ضوئي ذكي" كاملة داخل التطبيق — شبيهة بماسح المستندات في
واتساب — تكتشف حواف الورقة/الفاتورة تلقائياً، تصحّح منظورها، وتحوّلها إلى
PDF نظيف يُرفع إلى Supabase Storage، دون أي مكتبة رؤية حاسوبية خارجية
(لا OpenCV.js ولا أي حزمة جديدة — Canvas 2D + رياضيات مصفوفية خالصة فقط).

### 1) اكتشاف الحواف والتتبّع الحي (Edge Detection)

- وحدات جديدة تحت `lib/document-scanner/`:
  - `geometry.ts`: نقاط/شبه منحرف، حساب مصفوفة تحويل المنظور (Homography)
    عبر حل نظام معادلات خطية (Gaussian Elimination)، معكوس مصفوفة 3×3،
    و`warpPerspective` (تصحيح منظور + اقتصاص تلقائي عبر أخذ عيّنة ثنائية
    الخطية/Bilinear لكل بكسل هدف).
  - `detect-document.ts`: تصغير الإطار، تدرّج رمادي، عتبة **Otsu** تلقائية،
    Flood Fill تكراري لأكبر منطقة "ورقة" متصلة، وتقريب حوافها الأربع عبر
    أقصى/أدنى قيم (x+y)/(x-y).
  - `enhance.ts`: رفع تباين/سطوع خفيف لمظهر "ماسح ضوئي" نظيف بعد التصحيح.
  - `to-pdf.ts`: تحويل الكانفاس النهائي مباشرة إلى ملف PDF (Blob) بحجم صفحة
    مطابق لأبعاد المستند نفسه (لا A4 ثابت وهوامش بيضاء زائدة).
- مكوّن جديد `components/scanner/DocumentScannerModal.tsx`: كاميرا حية
  (`getUserMedia`) بملء الشاشة + إطار تفاعلي متوهّج فوق الفيديو (سماوي أثناء
  البحث، أخضر عند الثبات) يتتبّع حواف المستند ~7 مرات/ثانية، **التقاط تلقائي**
  عند ثبات الإطار لعدة قراءات متتالية، أو **زر التقاط يدوي** كبير في أي وقت،
  شاشة معاينة للنتيجة المصحَّحة مع "إعادة المحاولة"/"استخدام هذا المستند"،
  وخيار احتياطي "اختيار من المعرض" (يمرّ بنفس خط أنابيب الاكتشاف/التصحيح)
  عند تعذّر الوصول إلى الكاميرا.

### 2) التحويل إلى PDF والرفع لـ Supabase Storage

- بعد التأكيد، يُنتَج من نفس الكانفاس المصحَّح: صورة JPEG نظيفة (لعرضها في
  المعاينة المصغّرة والأرشيف كما هو معتاد) + ملف PDF فعلي (لأرشفة رسمية).
- `app/page.tsx`: دالة `handleDocumentCaptured` الجديدة (بديل `handleFileUpload`
  القديمة) ترفع كِلا الملفين إلى نفس مسار `invoices-images` في Supabase
  Storage (`{userId}/{timestamp}.jpg` و`{timestamp}.pdf`)، ثم تُدرج سجل
  الفاتورة بـ `image_url` كما كان تماماً — **بدون أي تغيير في مخطط قاعدة
  البيانات** (لا حاجة لعمود جديد)؛ فشل رفع نسخة PDF الأرشيفية تحديداً غير
  حرج لأن زرّي "تنزيل/طباعة PDF" الموجودين مسبقاً في الواجهة يعيدان توليد
  نفس الملف عند الطلب من الصورة المصحَّحة أساساً.
- الزر العائم "+" الأساسي أصبح يفتح `DocumentScannerModal` مباشرة بدل
  فتح كاميرا الجهاز الافتراضية.

### 3) الحفاظ على الهوية البصرية

- نفس نظام الألوان (سماوي/زمردي)، خط Cairo، الأرقام الإنجليزية البارزة
  (`.tnum`)، والزوايا الدائرية/الظلال الناعمة المستخدمة في بقية التطبيق.

### الفحص والتأكد

- ✅ `npx tsc --noEmit` — بدون أخطاء أنواع.
- ✅ `npx eslint` على كل ملفات الميزة الجديدة — بدون أخطاء أو تحذيرات.
- ✅ `npm run build` — بناء إنتاجي ناجح بالكامل.

---

## [UI] تحديث الخطوط والتصميم العام — معايير تصميم 2027

**التاريخ:** 28 يوليو 2026

### الهدف

تحديث شامل لواجهات التطبيق (UI/UX) على مستوى الخطوط، الأرقام، والاستجابة
التلقائية للشاشات، ليواكب التطبيق أحدث معايير تصميم تطبيقات SaaS العصرية:
وضوح أكبر للنصوص العربية، أرقام إنجليزية بارزة للتواريخ والمبالغ والعدادات،
وتخطيط مرن يستغل المساحة بذكاء على كل أحجام الشاشات.

### 1) الخطوط العربية

- إضافة خط **Cairo** (خط عربي/لاتيني عصري متعدد الأوزان — Variable Font) عبر
  `next/font/google` في ملف جديد `app/fonts.ts`. يُحمَّل الخط ذاتياً
  (self-hosted) بدون أي طلبات خارجية من المتصفح إلى خوادم Google — أداء أفضل
  وخصوصية أعلى.
- ربط الخط عالمياً عبر متغيّر CSS (`--font-cairo`) على وسم `<html>` في
  `app/layout.tsx`، ومن ثم في `--font-sans` ضمن `@theme` في
  `app/globals.css` — بحيث يغطي **كامل** التطبيق تلقائياً دون تكراره ملفاً
  بملف.
- تكبير حجم الأساس (`html { font-size: 16px }`) وضبط تباعد الأسطر
  (`line-height: 1.65` للنص العادي، `1.35` للعناوين) لقراءة أوضح وأكثر
  راحة للعين، ومنع تداخل الحروف/الحركات العربية.
- تكبير العناوين الرئيسية والنصوص الأساسية في كل الشاشات: شاشة تسجيل
  الدخول/إنشاء الحساب، شاشة تأكيد رمز الـ OTP ولوحة المفاتيح المخصصة، لوحة
  التطبيق الرئيسية (الهيدر، القوائم، البطاقات)، مودال إعدادات الخياط،
  وعارض الفواتير المكبّر (Lightbox). تم رفع معظم النصوص متناهية الصغر
  (`text-[10px]`/`text-[11px]`) إلى أحجام أوضح (`text-sm`/`text-base`)
  في كل الشاشات الموجّهة للمستخدم النهائي.

### 2) الأرقام الإنجليزية (تواريخ، هواتف، عدادات)

- إضافة فئة CSS مخصصة `.tnum` في `app/globals.css`:
  `font-variant-numeric: tabular-nums` + `direction: ltr` + `unicode-bidi: isolate`
  لعرض أرقام لاتينية ثابتة المحاذاة ومعزولة بصرياً عن اتجاه النص العربي
  المحيط بها.
- تطبيقها مع `font-bold`/`font-mono` على: تواريخ الفواتير (`formatDate`)،
  أرقام هواتف الخياط والعميل، عدّاد إجمالي الفواتير، ترقيم كل فاتورة
  (`#1`, `#2`, ...)، عدّاد عارض الصور المكبّر، وخانات ومؤقّت رمز الـ OTP —
  جميعها الآن أكبر، أعرض، وأبرز بصرياً.

### 3) الاستجابة التلقائية للشاشات (Responsive)

- الحاوية الرئيسية للوحة التطبيق أصبحت تتمدد تدريجياً حسب حجم الشاشة:
  `max-w-lg` (جوال) → `sm:max-w-2xl` (تابلت) → `lg:max-w-4xl` (شاشات كبيرة)،
  بدلاً من عمود ثابت ضيق يترك فراغات بيضاء ضخمة على الشاشات الكبيرة. الهيدر
  العلوي يحاذي نفس عرض المحتوى.
- "لوحة الخياط" و"رقم هاتف العميل" أصبحا عمودين جنباً إلى جنب
  (`sm:grid-cols-2`) بدل التكديس الرأسي الدائم.
- أرشيف الفواتير القديمة تحوّل من قائمة رأسية طويلة إلى **شبكة متجاوبة**
  (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`) لاستغلال أفضل للمساحة بدل
  التمرير الطويل على الشاشات الأوسع.
- زيادة أهداف اللمس (ارتفاعات/حشوات الأزرار والحقول والقوائم المنسدلة)
  لتناسب الاستخدام باللمس على الجوال بشكل أفضل.
- تكبير معاينة صورة الفاتورة ومعاينة الـ Lightbox تدريجياً حسب حجم الشاشة
  (`h-96 sm:h-[28rem] lg:h-[32rem]`, `max-w-lg lg:max-w-2xl`).

### الملفات المتأثرة

- `app/fonts.ts` (جديد) — تحميل خط Cairo عبر `next/font/google`.
- `app/layout.tsx` — ربط متغيّر الخط بوسم `<html>`.
- `app/globals.css` — حجم الأساس، تباعد الأسطر، متغيّر `--font-sans`، فئة `.tnum`.
- `app/page.tsx` — شاشة المصادقة ولوحة التطبيق الرئيسية بالكامل (خطوط، أرقام، استجابة).
- `app/auth/callback/page.tsx` — توحيد الخطوط مع باقي شاشات المصادقة.
- `components/auth/AuthModeTabs.tsx`, `OtpCodeInput.tsx`, `OtpKeypad.tsx`,
  `AuthConfirmationPanel.tsx`, `AuthBootScreen.tsx` — تكبير الخطوط وتطبيق `.tnum`.

### الفحص والتأكد

- ✅ `npx tsc --noEmit` — بدون أخطاء أنواع (TypeScript).
- ✅ `npm run build` — بناء إنتاجي ناجح بالكامل (بما في ذلك تحميل خط Cairo أثناء البناء).
- ✅ `npx eslint` — لا أخطاء جديدة (الملاحظات المتبقية سابقة الوجود ولا علاقة لها بهذا التحديث).
- ✅ اختبار تشغيلي مباشر (`next dev` + `curl`) أكّد نجاح عرض الصفحة (HTTP 200)
  وتطبيق خط Cairo فعلياً على وسم `<html>`.
