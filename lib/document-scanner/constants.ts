/** إعدادات مشتركة لخط أنابيب الماسح الضوئي الذكي (اكتشاف/تتبّع/التقاط). */

/** عرض الكانفاس المصغّر المستخدم لاكتشاف الحواف في كل إطار حي (كلما صغر، زادت السرعة). */
export const DETECTION_SAMPLE_WIDTH = 220;

/**
 * عرض كانفاس الاكتشاف عالي الدقة المستخدم لحظة الالتقاط فقط (يدوياً أو
 * تلقائياً) — أكبر بكثير من دقة الحلقة الحية لضمان تحديد حواف دقيق جداً في
 * اللحظة الفعلية للالتقاط، مع تفعيل الإغلاق المورفولوجي (denoise) لأنه
 * يُشغَّل مرة واحدة فقط فلا يؤثر على أداء المعاينة الحية.
 */
export const CAPTURE_DETECTION_SAMPLE_WIDTH = 640;

/** الفاصل الزمني بين محاولات الاكتشاف أثناء المعاينة الحية (تقريباً 7 مرات/ثانية). */
export const DETECTION_INTERVAL_MS = 140;

/** عدد الاكتشافات المتتالية المطلوبة قبل اعتبار الإطار "مستقراً" لبدء الالتقاط التلقائي. */
export const STABILITY_FRAME_COUNT = 5;

/** نسبة الدمج (Exponential Moving Average) لتنعيم حركة زوايا الإطار التفاعلي بين الإطارات؛ كلما قلّت، زاد الثبات وقلّ الاهتزاز. */
export const OVERLAY_SMOOTHING_ALPHA = 0.35;

/** عدد محاولات الاكتشاف الفاشلة المتتالية المسموح بها قبل إخفاء الإطار التفاعلي تماماً (يمنع الوميض في الإضاءة الضعيفة/الخلفية المضطربة). */
export const MAX_MISSED_DETECTION_STREAK = 4;

/**
 * أقل/أعلى نسبة تغطية معقولة لمستطيل الورقة داخل الإطار (لرفض اكتشافات فارغة/خاطئة).
 * الحدّ الأعلى مقصود أن يكون متحفّظاً نسبياً: عندما تكون الخلفية (أرضية/طاولة
 * فاتحة) قريبة الإضاءة من الورقة، يميل الاكتشاف لدمجهما في كتلة واحدة ضخمة
 * تكاد تغطي الإطار كاملاً — رفض أي كتلة كبيرة جداً هنا يمنع اعتمادها خطأً
 * كمستند (بدل قصّ الخلفية بأكملها معه).
 */
export const MIN_COVERAGE_RATIO = 0.08;
export const MAX_COVERAGE_RATIO = 0.7;

/** أقل نسبة "امتلاء" (عدد بكسلات الكتلة ÷ مساحة شبه منحرفها الهندسي) لاعتبارها ورقة مستطيلة صلبة، لا بقعة ضجيج متناثرة. */
export const MIN_BLOB_FILL_RATIO = 0.55;

/** أقل حجم لكتلة مرشَّحة (كنسبة من مساحة الإطار) حتى تُحفَظ للمقارنة أصلاً — يتجاهل بقع الضجيج الصغيرة جداً باكراً. */
export const MIN_CANDIDATE_BLOB_RATIO = 0.02;

/**
 * أعلى متوسط "تدرّج/نسيج" (Gradient Magnitude) مسموح به داخل منطقة الكتلة
 * المختارة (مُقاسة على شبكة نقاط في وسط شبه المنحرف، بعيداً عن حوافه) حتى
 * تُقبل كورقة حقيقية. سطح الورقة الداخلي غالباً مسطّح (نص/خطوط رفيعة متناثرة)
 * فمتوسط تدرّجه منخفض، بينما خلفية مثل أرضية بلاط/سيراميك تحتوي فواصل/نقوش
 * كثيفة فمتوسط تدرّجها أعلى بكثير. تُستخدم كخط دفاع أخير لرفض كتلة "ورقة+خلفية
 * مدموجتين" حتى لو اجتازت فحوصات التغطية والامتلاء أعلاه.
 */
export const MAX_INTERIOR_TEXTURE = 30;

/**
 * عند تعذّر اكتشاف حواف المستند بثقة كافية، نتراجع لإطار مركزي آمن أضيق من
 * كامل الإطار (بدل استخدام الكاميرا بكامل طولها/عرضها وما قد يظهر فيها من
 * أرضية/أثاث/أطراف الجسم) — هذه النسب تحدّد الهامش المتروك من كل جهة.
 */
export const FALLBACK_MARGIN_X_RATIO = 0.1;
export const FALLBACK_MARGIN_Y_RATIO = 0.07;

/** أعلى بُعد (طول الضلع الأطول بالبكسل) للمستند النهائي بعد تصحيح المنظور، لضبط حجم الملف والأداء. */
export const MAX_OUTPUT_DIMENSION = 1700;

/** جودة ضغط JPEG المستخدمة لمعاينة المستند الممسوح فقط (المعاينة داخل الماسح، وليست ملف PDF النهائي). */
export const SCAN_JPEG_QUALITY = 0.92;

/* ----------------------------------------------------------------------- */
/* ثوابت اكتشاف/تصحيح المنظور/تحسين عبر OpenCV.js (رؤية حاسوبية حقيقية) —   */
/* المسار الأساسي الجديد؛ يتراجع الماسح تلقائياً لخط الأنابيب اليدوي أعلاه   */
/* (Canvas 2D خالص) إن تعذّر تحميل OpenCV.js أو فشل الاكتشاف عبره.          */
/* ----------------------------------------------------------------------- */

/** عرض كانفاس العمل المستخدم لاكتشاف حواف OpenCV.js في الحلقة الحية (سرعة عالية). */
export const CV_DETECTION_SAMPLE_WIDTH = 260;

/** عرض كانفاس العمل المستخدم لاكتشاف حواف OpenCV.js عالي الدقة لحظة الالتقاط فقط. */
export const CV_CAPTURE_DETECTION_SAMPLE_WIDTH = 720;

/** عتبتا Canny الدنيا/العليا لاكتشاف الحواف (Edge Detection) — كلما اتسع المجال، اكتُشفت حواف أضعف أيضاً (وربما ضجيج أكثر). */
export const CV_CANNY_LOWER_THRESHOLD = 40;
export const CV_CANNY_UPPER_THRESHOLD = 120;

/** أقل نسبة (طول التقريب المضلّعي ÷ محيط المخطط) في `approxPolyDP` — كلما زادت، أصبح التقريب أكثر تسامحاً (زوايا أقل حدّة لكن أكثر ميلاً لأربعة أضلاع). */
export const CV_APPROX_POLY_EPSILON_RATIO = 0.02;

/** نصف قطر نافذة العتبة التكيّفية المحلية (Adaptive Threshold) عبر OpenCV.js — كنسبة من أصغر بُعد للصورة، مع حدّين أدنى/أقصى (يجب أن تكون فردية دوماً). */
export const CV_ADAPTIVE_BLOCK_SIZE_RATIO = 1 / 18;
export const CV_ADAPTIVE_BLOCK_SIZE_MIN = 15;
export const CV_ADAPTIVE_BLOCK_SIZE_MAX = 61;

/** ثابت الطرح `C` في العتبة التكيّفية لـ OpenCV.js — كلما زاد، أصبحت الخلفية أنظف (تسامحاً أقل مع الرمادي) لكن قد يفقد بعض الحبر الباهت جداً. */
export const CV_ADAPTIVE_THRESHOLD_C = 10;

/** جودة ضغط JPEG المستخدمة كبديل احتياطي فقط عند تعذّر تصدير PNG لأي سبب. */
export const PDF_FALLBACK_JPEG_QUALITY = 0.85;

/* ----------------------------------------------------------------------- */
/* ثوابت "تأثير الماسح الضوئي" (lib/document-scanner/enhance.ts) — معايرة   */
/* مركزية هنا لتسهيل أي ضبط لاحق (بعد اختبار ميداني على أسطح/إضاءات مختلفة) */
/* دون الحاجة لتعديل منطق الخوارزمية نفسه.                                  */
/* ----------------------------------------------------------------------- */

/** مقدار إزاحة عتبة التباين النهائية نحو الأسفل: كلما زاد، أصبح تصنيف بكسل كـ"حبر أسود" أكثر صرامة (خلفية أنظف، نص أقل دكانة قليلاً). */
export const SCAN_INK_BIAS = 22;

/** حدّة منحنى التباين (Sigmoid) النهائي حول العتبة؛ كلما زادت، ازداد التباين حدّة (وربما ازداد الضجيج قرب نقطة العتبة). */
export const SCAN_CONTRAST_STEEPNESS = 0.24;

/** أقل/أعلى حجم (بالبكسل) لبقعة سوداء معزولة حتى تُعتبر "ضجيجاً" فتُمحى إلى الأبيض في التنظيف النهائي (Despeckle). */
export const SCAN_DESPECKLE_MIN_SIZE = 6;
export const SCAN_DESPECKLE_MAX_SIZE = 40;
/** نسبة حجم البقعة المُزالة من إجمالي عدد بكسلات الصورة (قبل التقييد بالحدّين أعلاه). */
export const SCAN_DESPECKLE_SIZE_RATIO = 0.000012;
